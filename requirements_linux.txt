fastapi~=0.115.6
uvicorn
sse_starlette
apscheduler
websockets
python-multipart

# Linux 专用依赖
pyaudio==0.2.14; sys_platform == 'linux' and platform_machine == 'x86_64'
webrtcvad==2.0.10; sys_platform == 'linux' and platform_machine == 'x86_64'

# 阿里云 SDK 使用新版
aliyun-python-sdk-core-v3==2.15.0  # 使用 v3 版本替代旧版
crcmod==1.7  # 使用兼容版本

# FunASR 使用官方源
funasr>=0.8.5  # 从 PyPI 安装而不是直接下载 wheel